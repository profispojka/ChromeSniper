{
  "name": "snipper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "build:prod": "node build.mjs --prod",
    "watch": "node build.mjs --watch",
    "package": "node build.mjs --prod --package",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "qrcode-generator": "^2.0.4",
    "sharp": "^0.34.5",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "esbuild": "^0.28.0"
  }
}
